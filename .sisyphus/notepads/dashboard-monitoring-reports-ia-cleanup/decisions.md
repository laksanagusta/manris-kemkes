## 2026-04-03
- Renamed the page title to `Monitoring & Updates` to better match the operational follow-up role of the workspace and avoid overlap with reporting-focused screens.
- Added the `/reports` secondary CTA to the monitoring header using `Button asChild` + `Link` so the new path stays explicit and accessible without altering destinations.
